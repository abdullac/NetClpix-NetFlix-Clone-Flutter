import 'package:netclipxsample/presentations/core/functions/dimonsions.dart';

var searchedImageAreaDimonsion = screenDimonsion(screenWidth() * 30 / 100,
    screenWidth() * 7 / 100, screenWidth() * 12 / 100);

var movieTitleFontSize = screenDimonsion(
    screenWidth() * 5 / 100, screenWidth() * 2 / 100, screenWidth() * 3 / 100);

var playIconAreaDimonsion = screenDimonsion(screenWidth() * 5.55 / 100,
    screenHeight() * 3.55 / 100, screenWidth() * 3.55 / 100);
var playIconSize = screenDimonsion(screenWidth() * 12.5 / 100,
    screenHeight() * 8 / 100, screenWidth() * 8 / 100);
